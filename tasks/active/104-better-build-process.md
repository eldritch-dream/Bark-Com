# Better build process

**Category:** Tooling
**Source:** TODO.txt (Tooling)
**Original note:** "Better build process"

Vague — needs definition. Candidate scope: reduce build script branching, separate dev/release configs, more reliable CI feedback. See `build_game.ps1`, `release.ps1`, and `.github/`.
