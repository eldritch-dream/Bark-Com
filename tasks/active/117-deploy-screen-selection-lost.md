# Deploy screen loses selection on leave and return

**Category:** Bug
**Source:** TODO.txt (Bugs)
**Original note:** "selecting a unit in the deploy screen should stay selected even when leaving and returning to the screen"

Selection state on the deploy screen resets when navigating away and back. Persist it on the screen's controller.
