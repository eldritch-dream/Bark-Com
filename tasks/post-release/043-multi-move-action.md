# Use 2+ moves at once (with helper UI and obvious extra AP)

**Category:** Feature
**Source:** TODO.txt
**Original note:** "add ability to use 2+ moves at once (needs visual helper and obvious extra ap usage for user)"

Allow chaining multiple move actions in one planning step, with a visual helper that shows the extended path and the AP it costs.
